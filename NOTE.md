# Describe usage of echo-estimate package, e.g. it uses other unpublished packages

## Add chapter on unit tests

Unittest function `GetInterfaces` in `utils` package.

## Add chapter on handling signals
